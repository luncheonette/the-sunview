[build]
  command = "bundle exec jekyll build"
  publish = "_site"

[build.environment]
  # Use Ruby version from .ruby-version file
  RUBY_VERSION = "3.3.10"
  # Skip Node.js entirely
  NODE_VERSION = ""
